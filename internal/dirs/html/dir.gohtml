<!DOCTYPE html>
<html>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">

    <link href="/css/dir.css" rel="stylesheet">

    <title>{{printf "%q" .Path}}</title>

    <head></head>

    <body>
        <table id="list_table">
            <thead>
                <tr>
                    <th>{{.Path}}</th>
                </tr>
            </thead>
            <tbody>{{range $ent := .Entries}}
                <tr>
                    <th>{{if $ent.IsDir}}
                        <a class="file dir" href="{{$ent.Name}}/"></a>📁 {{else}}
                        <a class="file" href="{{$ent.Name}}"></a>📄 {{end}}{{$ent.Name}}
                    </th>
                </tr>{{end}}
                <tr>
                    <th>
                        <form enctype="multipart/form-data" action="{{.Path}}?upload" method="post">
                            <input type="file" name="upload_file" />
                            <input type="submit" value="upload" />
                        </form>
                        ➕ Upload
                    </th>
                </tr>
            </tbody>
        </table>

        <table id="data_table">
            <thead>
                <tr>
                    <td>Mode</td>
                    <td>Size</td>
                    <td>Last Modified</td>
                </tr>
            </thead>
            <tbody>{{range $ent := .Entries}}
                <tr>
                    <td>{{formatMode $ent.Mode}}</td>
                    <td>{{if not $ent.IsDir}}{{formatSize $ent.Size}}{{end}}</td>
                    <td>{{formatTime $ent.ModTime}}</td>
                </tr>{{end}}
            </tbody>
        </table>
    </body>
</html>
